<Ui xmlns="http://www.blizzard.com/wow/ui/"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.blizzard.com/wow/ui/ ..\FrameXML\UI.xsd">

    <!-- BarWardenBarTemplate: A single tracking bar (StatusBar with icon, text overlays, spark, background) -->
    <StatusBar name="BarWardenBarTemplate" virtual="true" minValue="0" maxValue="1" defaultValue="0">
        <Size x="200" y="20"/>
        <Layers>
            <!-- Background layer -->
            <Layer level="BACKGROUND">
                <Texture name="$parentBackground" setAllPoints="true">
                    <Color r="0" g="0" b="0" a="0.6"/>
                </Texture>
            </Layer>
            <!-- Border layer -->
            <Layer level="BORDER">
                <Texture name="$parentBorder" setAllPoints="true">
                    <Color r="0" g="0" b="0" a="0.8"/>
                </Texture>
            </Layer>
            <!-- Overlay layer: text elements -->
            <Layer level="OVERLAY">
                <FontString name="$parentNameText" inherits="GameFontHighlightSmall" justifyH="LEFT">
                    <Anchors>
                        <Anchor point="LEFT" x="24" y="0"/>
                        <Anchor point="RIGHT" x="-40" y="0"/>
                    </Anchors>
                </FontString>
                <FontString name="$parentTimeText" inherits="GameFontHighlightSmall" justifyH="RIGHT">
                    <Anchors>
                        <Anchor point="RIGHT" x="-4" y="0"/>
                    </Anchors>
                </FontString>
            </Layer>
        </Layers>
        <Frames>
            <!-- Icon frame on the left -->
            <Frame name="$parentIcon">
                <Size x="20" y="20"/>
                <Anchors>
                    <Anchor point="LEFT" x="1" y="0"/>
                </Anchors>
                <Layers>
                    <Layer level="ARTWORK">
                        <Texture name="$parentIconTexture" setAllPoints="true"/>
                    </Layer>
                </Layers>
            </Frame>
            <!-- Spark texture (bright line at the bar's current value) -->
            <Frame name="$parentSparkFrame" frameLevel="3">
                <Size x="16" y="20"/>
                <Layers>
                    <Layer level="OVERLAY">
                        <Texture name="$parentSpark" file="Interface\CastingBar\UI-CastingBar-Spark">
                            <Size x="16" y="32"/>
                            <Anchors>
                                <Anchor point="CENTER"/>
                            </Anchors>
                        </Texture>
                    </Layer>
                </Layers>
            </Frame>
        </Frames>
    </StatusBar>

    <!-- BarWardenGroupTemplate: Container frame for a group of bars with backdrop -->
    <Frame name="BarWardenGroupTemplate" virtual="true" enableMouse="true" movable="true" clampedToScreen="true">
        <Size x="210" y="100"/>
        <Backdrop bgFile="Interface\Tooltips\UI-Tooltip-Background" edgeFile="Interface\Tooltips\UI-Tooltip-Border" tile="true">
            <EdgeSize val="12"/>
            <TileSize val="16"/>
            <BackgroundInsets left="2" right="2" top="2" bottom="2"/>
        </Backdrop>
        <Layers>
            <Layer level="OVERLAY">
                <FontString name="$parentTitle" inherits="GameFontNormal" justifyH="CENTER">
                    <Anchors>
                        <Anchor point="TOP" x="0" y="-4"/>
                    </Anchors>
                </FontString>
            </Layer>
        </Layers>
    </Frame>

    <!-- BarWardenTabButtonTemplate: Tab button for the config panel -->
    <Button name="BarWardenTabButtonTemplate" inherits="CharacterFrameTabButtonTemplate" virtual="true">
        <Size x="0" y="32"/>
    </Button>

</Ui>
